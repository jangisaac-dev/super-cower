interface:
  display_name: "Super Cower"
  short_description: "Codex-native software workflow with subagent orchestration"
  default_prompt: "Inspect the repo, clarify only if needed, plan explicitly for multi-step work, and finish this software task with the super-cower workflow."

policy:
  allow_implicit_invocation: true
